"use strict";
// Everything pertaining to map generation, rendering

class GameMap{
    constructor(){
        //init
        this.map = window.mapData;
    }
    tick(){
        // tick
    }
}